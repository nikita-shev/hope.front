import { CSSProperties } from 'react';

export interface IControlInlineStyles  {
    wrap?: CSSProperties;
    input?: CSSProperties;
    text?: CSSProperties;
}

export interface IChoiceControlsStyles extends IControlInlineStyles {} // delete
